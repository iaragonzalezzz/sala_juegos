{
  "version": 2,
  "builds": [
    {
      "src": "**/*.ts",
      "use": "@vercel/angular",
      "config": { 
        "buildTarget": "production"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}